<Window x:Class="SevenDaysModLauncher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SevenDaysModLauncher.ViewModels"
        mc:Ignorable="d"
        Title="7 Days to Die Mod Launcher"
        Height="600"
        Width="900"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Game Folder Section -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

<TextBlock Grid.Column="0"
                        Text="Game Folder:"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource ForegroundBrush}"
                        Margin="0,0,8,0"/>

            <TextBox Grid.Column="1"
                     Text="{Binding GameFolderPath, Mode=OneWay}"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center"
                     Padding="8,4"
                     Background="{DynamicResource ControlBackgroundBrush}"
                     Foreground="{DynamicResource ForegroundBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"/>

<Button Grid.Column="2"
                     Content="Browse..."
                     Command="{Binding BrowseGameFolderCommand}"
                     Margin="8,0,0,0"
                     Padding="16,4"
                     Style="{DynamicResource AccentButtonStyle}"/>
<Button Grid.Column="3"
                     Content="Auto"
                     Command="{Binding AutoDetectGameFolderCommand}"
                     Margin="8,0,0,0"
                     Padding="16,4"
                     Style="{DynamicResource AccentButtonStyle}"/>
        </Grid>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
<Button Content="Install Mod"
                     Command="{Binding InstallModCommand}"
                     Padding="16,6"
                     Style="{DynamicResource AccentButtonStyle}"
                     IsEnabled="{Binding IsNotBusy}"/>

<Button Content="Refresh List"
                     Command="{Binding RefreshModsCommand}"
                     Padding="16,6"
                     Margin="8,0,0,0"
                     IsEnabled="{Binding IsNotBusy}"/>
        </StackPanel>

        <!-- Mods List -->
        <Border Grid.Row="2"
                Background="{DynamicResource ControlBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" Height="32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

<TextBlock Grid.Column="0" Text="On" Margin="8,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold"/>
<TextBlock Grid.Column="1" Text="Mod Name" Margin="8,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold"/>
<TextBlock Grid.Column="2" Text="Version" Margin="8,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold"/>
<TextBlock Grid.Column="3" Text="Actions" Margin="8,0" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontWeight="SemiBold"/>
                </Grid>

                <!-- List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Mods}" Margin="0,4">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Command="{Binding DataContext.ToggleModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}"/>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   Margin="8,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource ForegroundBrush}"/>

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Version, TargetNullValue='—'}"
                                                   Margin="8,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource SecondaryForegroundBrush}"/>

<Button Grid.Column="3"
                                                 Content="Delete"
                                                 Command="{Binding DataContext.DeleteModCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                 CommandParameter="{Binding}"
                                                 Padding="8,2"
                                                 Margin="4,0"
                                                 Style="{DynamicResource DangerButtonStyle}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Empty state -->
<TextBlock Grid.Row="1"
                            Text="No mods found. Install a mod or select the game folder."
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource SecondaryForegroundBrush}"
                            Visibility="{Binding Mods.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Progress -->
        <Grid Grid.Row="3" Margin="0,8,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar Value="{Binding ProgressValue, Mode=OneWay}"
                         Maximum="1"
                         Height="20"
                         Background="{DynamicResource ControlBackgroundBrush}"
                         Foreground="{DynamicResource AccentBrush}"/>
            <TextBlock Text="{Binding StatusMessage}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource ForegroundBrush}"/>
        </Grid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="4"
                   Text="{Binding StatusMessage}"
                   Margin="0,8,0,0"
                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                   TextTrimming="CharacterEllipsis"/>
    </Grid>
</Window>
